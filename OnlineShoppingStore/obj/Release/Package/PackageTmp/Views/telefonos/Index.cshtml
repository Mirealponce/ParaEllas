@model IEnumerable<OnlineShoppingStore.Models.DAL.telefono>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Ntelefono)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cliente.nombre)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Ntelefono)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cliente.nombre)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.idtelefono }) |
                @Html.ActionLink("Details", "Details", new { id=item.idtelefono }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.idtelefono })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
